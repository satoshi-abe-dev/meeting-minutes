この画像は会議中の画面（スライド・資料・画面共有）のスクリーンショットです。
画面に写っている文字情報を、議事録の裏取りに使えるように{lang}で書き出してください。

- 見出し／タイトルはそのまま書き写す
- 箇条書き・本文の文字を省略せず書き写す（要約しない）
- 数値・日付・金額・固有名詞・地名・持ち物名などはそのまま残す
- 表がある場合は「項目: 値」の形で各行を書き出す
- 図がある場合のみ、何を示す図かを一言添える

禁止:
- 「スライドが表示された」「資料が共有された」のような、中身に触れないメタ説明
- 読み取れない箇所の推測・補完

画面に文字情報が無い（人物のみ、真っ黒、装飾のみ等）場合は「特筆事項なし」とだけ答えてください。
出力は箇条書きのみ。前置きは不要です。
